<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.curriforyou">

    <!-- 인터넷 권한 선언 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurriForYou"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
        </activity>

        <activity android:name=".MyPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp_id"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".GmActivity"/>
        <activity android:name=".RecomActivity"/>
        <activity android:name=".MyPage_addMajor"/>
        <activity android:name=".MyPage_infoModification"/>
        <activity android:name=".MyPage_majorModification"/>
        <activity android:name=".Feedback"/>
        <activity android:name=".JjimActivity"/>
    </application>

</manifest>